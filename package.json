{
  "name": "kimpton-ai-public",
  "version": "1.0.0",
  "description": "Clean-room TypeScript web app demonstrating a synthetic AI-native investment research workspace.",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "npm run build && node dist/tests/workspace.test.js",
    "validate": "npm run test && node dist/server.js --smoke"
  },
  "keywords": [
    "clean-room",
    "typescript",
    "investment-research",
    "synthetic-data",
    "mock-integrations"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
